#!/bin/bash


bash -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"

list_of_plugins=(brew bundler cask dotenv git history nomad osx rake rbenv ruby terraform vagrant vagrant-prompt vault vscode)
